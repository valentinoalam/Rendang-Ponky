export type ShippingOption = {
    service: string
    description: string
    cost: number
    etd: string
    courier: string
  }
  
  export type Destination = {
    city: string
    postalCode: string
  }
  